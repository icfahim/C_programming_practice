#include <stdio.h>
int main ()
{
    int i,n;
    int a[n],sum,avg;
    scanf("%d",&a[i]);
    for (i=0;i<n;i++)
    {
        sum+=a[i];
    }
    avg=sum/n;
    printf("%d\n%d",sum,avg);
    return 0;
}
