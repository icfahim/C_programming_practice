#include <stdlib.h>
int main ()
{
    int count =0;
    char *p;
    p=malloc(100);
    gets(p);
    print :
        printf("%d",count);

{        p++;
        count++;}
        if(*p!='\0')
            goto print;
        return 0;
}
